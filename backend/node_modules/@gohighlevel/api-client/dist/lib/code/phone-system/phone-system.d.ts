import { AxiosInstance, AxiosRequestConfig } from 'axios';
/**
 * PhoneSystem Service
 * Documentation for Phone System API
 */
export declare class PhoneSystem {
    private client;
    constructor(httpClient: AxiosInstance);
    /**
     * List Number Pools
     * Get list of number pools
     */
    getNumberPoolList(params: {
        locationId?: string;
    }, options?: AxiosRequestConfig): Promise<any>;
    /**
     * List active numbers
     * Retrieve a paginated list of active phone numbers for a specific location. Supports filtering, pagination, and optional exclusion of number pool assignments.
     */
    activeNumbers(params: {
        locationId: string;
        pageSize?: number;
        page?: number;
        searchFilter?: string;
        skipNumberPool?: boolean;
    }, options?: AxiosRequestConfig): Promise<any>;
}
export default PhoneSystem;
//# sourceMappingURL=phone-system.d.ts.map